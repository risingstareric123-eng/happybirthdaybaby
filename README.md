# happybirthdaybaby

<!--
CloudBase 部署注意事项：
1) CloudBase Web SDK 需要在「云开发平台 -> 环境配置 -> 安全配置」添加站点域名（例如：risingstareric123-eng.github.io），否则会出现 CORS / 鉴权失败。
2) 安全域名配置存在生效延迟，修改后请等待一段时间再重试。
3) 请使用 HTTPS 访问页面（GitHub Pages 默认 HTTPS）。
-->
